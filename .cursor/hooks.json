{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "command": "bash -c 'FILE=$(jq -r .tool_input.file_path) && [ -n \"$FILE\" ] && npx eslint --fix --quiet \"$FILE\"'",
        "timeout": 5000,
        "matcher": "Write|StrReplace"
      }
    ]
  }
}
